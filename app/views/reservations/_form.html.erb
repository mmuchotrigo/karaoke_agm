<%= simple_form_for @reservation, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :reserved_at, :label => "Fecha y Hora" %>
  <%= error_span(@reservation[:reserved_at]) %>
  <%= f.input :description, :label => "Descripción" %>
  <%= error_span(@reservation[:description]) %>
  <%= f.association :sala , :label => "Sala" %>
  <%= error_span(@reservation[:sala]) %>
  <%= f.association :user, :include_blank => false, :label_method => lambda { |user| "#{user.name} #{user.ap_paterno}" }, :label => "Cliente" %>
  <%= error_span(@reservation[:user]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                reservations_path, :class => 'btn btn-default' %>
<% end %>
